using Timer = System.Windows.Forms.Timer;

namespace SpaceGame.architecture;

public class Variables //TODO transport all variables (as like sizes, intervals etc.) into class
{
    public static Timer? Timer { get; set; }
}